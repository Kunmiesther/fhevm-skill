import { task } from "hardhat/config";

// Placeholder task to satisfy hardhat.config.ts imports.
task("fhecounter", "FHECounter helper task (placeholder)", async () => {
  // Intentionally empty.
});

